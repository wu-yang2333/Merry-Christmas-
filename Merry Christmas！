<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>圣诞快乐</title>
    <style>
        body,
        html {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden;
            font-family: 'Arial', sans-serif;
            background: #f0f0f0 url('santa-hat.png') no-repeat center 20px;
            background-size: contain;
        }

        .snowflake {
            position: absolute;
            top: -10px;
            background-color: white;
            user-select: none;
            cursor: pointer;
            opacity: 0.8;
        }

        .snowflake:hover {
            opacity: 1;
        }

        @keyframes fall {
            0% {
                transform: translateY(0);
                opacity: 1;
            }

            100% {
                transform: translateY(100vh);
                opacity: 0;
            }
        }

        @keyframes sway {

            0%,
            100% {
                transform: rotate(-15deg);
            }

            50% {
                transform: rotate(15deg);
            }
        }

        .christmas-card {
            opacity: 0.9;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            text-align: center;
            max-width: 90%;
            width: 100%;
            margin: 10px;
            position: absolute;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
        }

        .christmas-card h1 {
            color: #FF4136;
            font-size: 2em;
            margin: 0;
        }

        .christmas-card p {
            color: #333;
            font-size: 1em;
            margin: 10px 0;
        }

        .christmas-card img {
            width: 100%;
            max-width: 200px;
            height: auto;
            border-radius: 5px;
            margin: 10px 0;
        }

        .christmas-card {
            text-align: center;
            /* 使所有内容居中 */
        }

        .music-container {
            margin: 20px 0;
            /* 上下添加外边距，以在图片和段落之间创建空间 */
        }

        .music-player {
            display: inline-block;
            /* 使图片成为行内块级元素，以便应用外边距和填充 */
            max-width: 100px;
            /* 可选：限制图片的最大宽度 */
            height: auto;
            /* 保持图片的宽高比 */
            cursor: pointer;
            /* 当鼠标悬停在图片上时显示为手形，表明可点击 */
        }
        .responsive-img {
            width: 100%; /* 宽度设置为父元素的100% */
            max-width: 500px; /* 最大宽度限制为500px，防止在大屏幕上过大 */
            height: auto; /* 高度自动调整，保持宽高比 */
        }
    </style>
</head>

<body>
    <audio loop id="audioPlayer">
        <source src="Jingle Bells .mp3" type="audio/mpeg">
        Your browser does not support the audio element.
    </audio>
    <img src="圣诞树.jpg" alt="圣诞树" class="responsive-img">
    <div class="christmas-card">
        <h1>圣诞快乐！</h1>
        <p>亲爱的姐姐，愿这个圣诞节给你们带来无尽的快乐和温馨。</p>
        <div class="music-container">
            <img src="图1.png" class="music-player" id="musicPlayer" onclick="toggleMusic()" alt="音乐播放器">
        </div>
        <p>愿你的每一天都像圣诞树一样光彩夺目，充满希望和喜悦。</p>
    </div>
    <script>
        function toggleMusic() {
            var audio = document.getElementById('audioPlayer');
            if (audio.paused) {
                audio.play();
                document.getElementById('musicPlayer').src = '图1.png';
            } else {
                audio.pause();
                document.getElementById('musicPlayer').src = '图2.png';
            }
        }

        function createSnowflake() {
            varflake = document.createElement('div');
            varflake.classList.add('snowflake');
            varflake.style.left = Math.random() * window.innerWidth + 'px';
            varflake.style.width = Math.random() * 20 + 5 + 'px';
            varflake.style.height = varflake.style.width;
            varflake.style.animationDuration = Math.random() * 5 + 3 + 's';
            varflake.style.animationName = 'fall';
            document.body.appendChild(varflake);

            varflake.addEventListener('animationend', function () {
                this.remove();
            });
        }

        setInterval(createSnowflake, 500);
    </script>
</body>

</html>
